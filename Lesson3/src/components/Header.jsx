import React from 'react';

const Header = (props) => {
    return (
        <div className="header">
            <span className="header__chatName">Чат 1</span>
        </div>
    );
};

export default Header;